package util;

import flash.display.DisplayObject;

class GameManager {

    // array of objects use for some things like drag target hit testing.
    public static var entities:Array<DisplayObject> = new Array<DisplayObject>();



}